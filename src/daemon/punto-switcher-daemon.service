[Unit]
Description=Punto Switcher keyboard helper (evdev/uinput)
Documentation=man:punto-switcher-daemon(1)
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/sg input -c /usr/bin/punto-switcher-daemon
Restart=always
RestartSec=1

[Install]
WantedBy=default.target
